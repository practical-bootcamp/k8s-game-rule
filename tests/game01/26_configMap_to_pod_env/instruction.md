In the '{{namespace}}' namespace, Create a ConfigMap named options with the value var5={{value1}} and a Pod named nginx that loads the value from the options ConfigMap into an environment variable called option.